const color = '#fafafa';

const css = `
.body {
  background-color: ${color};
}
`;

css.jsonx()
html.html()

// for loop
var app = ul {
  ...details.map((detail) => {
    li { key: detail.id, #{detail} }
  })
};
